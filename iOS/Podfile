# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Shot Tracker!' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Shot Tracker!
pod 'Firebase'
pod 'Firebase/Core'
pod 'Firebase/Database'
pod 'Firebase/Auth'
pod 'Firebase/Firestore'
pod 'FirebaseUI/Google'
pod 'GoogleSignIn'
#pod 'FBSDKLoginKit'
  target 'Shot Tracker!Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Shot Tracker!UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'Shot Tracker! WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Shot Tracker! WatchKit App

end

target 'Shot Tracker! WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Shot Tracker! WatchKit Extension

end
